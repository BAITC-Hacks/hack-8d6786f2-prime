# Проверки проекта

## Текущий прогон: архитектурные замечания

23.09.2026, Windows 11, Python 3.12.14, Node 24.19.0, pnpm 11.19.0, Edge. Код: [8579db7014ac5441d75523008106c7f4331f89a0](https://github.com/BAITC-Hacks/hack-8d6786f2-prime/commit/8579db7014ac5441d75523008106c7f4331f89a0), с сохранением нового QA-отчёта команды `40f58c4`. **107 backend/HTTP, 33 frontend и 25 браузерных проверок** прошли; TypeScript, production build и актуальность OpenAPI/типов проверены. Ключи при массовой приёмке отключены. Браузерный набор дополнительно проверяет 28 реальных HTTP-ответов клиентской схемой.

Оценочный набор: 28 запросов по всем 17 категориям, 24 запроса с совпадениями, 50 точных карточек. Fallback и живой OpenAI проверяются одинаковыми запросами; метрики относятся к локальному HTTP-пути через TestClient. Отдельный нагрузочный сценарий использует настоящую сеть localhost и отдельный процесс. Методика и команды находятся в [qa/README.md](../qa/README.md).

Живой OpenAI: 24 из 24 запросов с совпадениями завершились в режиме llm; 50/50 цитат опираются на источник, у 41/50 найден отличающий признак, у 9/50 явно отмечены ограничения. В 6 запросах сервер восстановил более конкретный набор фактов. p95 нового ответа — 1785,16 мс, кеша — 4,19 мс; fallback — 4,06 мс. Эти цифры относятся к последовательному локальному TestClient-прогону, не интернету клиента.

Нагрузка отдельного процесса, настоящий localhost HTTP: 100 одновременных fallback-запросов, 0 ошибок, p95 477,20 мс, максимум 480,76 мс. Конкурентность живого AI этим замером не проверяется. Полная таблица сценариев, методика и ограничения: [evaluation-report.md](evaluation-report.md).

Установка по README проверена через `scripts/setup.ps1` с новым Python-окружением `.venv` и lock-файлами; сборка frontend прошла. Затем новый интерпретатор запустил `run.py --no-browser --port 0 --data-dir <временная-папка>`: сайт, JS/CSS, health/options и базовый подбор прошли HTTP-проверку, 66 профилей, режим fallback. Использовался локальный кеш пакетов. Чистая установка на другом компьютере этим не подтверждается.

Карта замечаний: [architecture-review-resolution.md](architecture-review-resolution.md). Workflow CI добавлен, но [запуск 35860730722](https://github.com/BAITC-Hacks/hack-8d6786f2-prime/actions/runs/35860730722) **не стартовал**: GitHub сообщил «The job was not started because your account is locked due to a billing issue». Выполненных шагов нет. Владелец организации должен устранить проблему оплаты Actions и повторить workflow; успешная облачная проверка не заявляется. Репозиторий остаётся private, локальный запуск доступен получившему исходники.

## Исторический прогон: исправление объяснений caf7772

После UI-коммита `146b974` исправлены QA-Q01/Q02/Q03 и синхронизированы браузерные тесты. Результаты: **89 backend/HTTP, 33 frontend, 21 штатный браузерный сценарий + 1 новый отдельным прогоном**; TypeScript и production build успешны. Три свежих запроса OpenAI подтвердили исправление в режиме llm без кеша. Сценарии, измерения и ограничения: [explanation-quality.md](explanation-quality.md). Из корня запускайте `python -m pytest backend/tests qa -q` и `python qa/run_e2e.py`.

## Исторический прогон очистки b68da2d

23.09.2026, Windows, Python 3.12.14. Проверена версия 3.0.0 с семью параметрами кейса и тремя маршрутами API. Регистрация и администрирование удалены; их тесты заменены или удалены вместе с функциями. Уменьшение числа тестов относительно версии платформы связано с сокращением объёма.

| Проверка | Фактический результат |
| --- | --- |
| Backend и независимый HTTP-набор | 71 passed; отдельные временные базы, ключи модели отключены |
| Frontend unit/component | 22 passed |
| TypeScript и production build | Успешно |
| Полный браузерный набор | 21 passed; состояния, реальные запросы, альтернативы, 360/390/1280/1440 px, проверки axe |
| Совместимость прежней SQL-базы | Проверена на отдельной SQLite backup-копии; 66 профилей, публичная версия данных совпала |
| Относительные ссылки Markdown | Проверены, отсутствующих файлов нет |

Браузерный набор запускался через `python qa/run_e2e.py`, backend — `python -m pytest backend/tests qa/test_recommendation_http.py -q -p no:cacheprovider --basetemp <новая-папка>`. Frontend: `pnpm test`, `pnpm build`.

Один StarletteDeprecationWarning о взаимодействии TestClient/httpx не мешал прохождению тестов. Живой платный API при очистке не тестировался; качество объяснений, новый внешний LLM-запрос и публичный хостинг не подтверждаются этим прогоном. Открытые замечания находятся в [known-issues.md](known-issues.md).
