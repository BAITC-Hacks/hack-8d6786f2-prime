# Проверка интеграции и поставки

23.09.2026, Windows 11 x64, Python 3.12.14, Node 24.19, pnpm 11.19.0.

## Интеграция интерфейса

Frontend `998ea25` объединён с backend `c6e15d2`; проверенная интеграция опубликована в main как `3814aa2`.

- Frontend: 59 unit/component-тестов; TypeScript и production build прошли.
- Браузерный набор на объединённом коде: 25 passed, без пропусков, отдельный API/Vite и временная БД.
- Старое позитивное ожидание null-длительности в HTTP-наборе перенесено на флориста; бизнес-запрет для услуг присутствия сохранён и проверяется отдельно.

## Архитектурные изменения

- `pytest backend/tests qa/test_platform_http.py`: **156 passed** — 83 backend-проверки и 73 независимых HTTP-проверки.
- Включены проверки отсутствия побочных эффектов при импорте фабрики, совместной раздачи сайта/API, закрытости приватных файлов, пути данных EXE и сохранения локального конфига.
- В новой отдельной копии без `.venv` и `node_modules` выполнен `scripts/setup.ps1`: зависимости установлены, TypeScript/Vite собрали сайт. Из-за расположения инструментов в среде Codex использованы параметры `-Python` и `-Pnpm` с явными путями; обычная ветка запуска через npx отдельно не проверялась.

## Реальный Windows ZIP

Собран `scripts/build_windows.py` с PyInstaller 6.22.3. Проверялся распакованный архив, не исходный Python-процесс.

`scripts/verify_windows.py release/SatContractors-Windows.zip --browser`:

- EXE запущен из другой рабочей папки; путь распаковки содержит кириллицу и пробел.
- PATH приложения содержит только системный каталог Windows, без установленных Python и Node.
- Сайт, настоящий API, 66 импортированных профилей и административный доступ работают.
- `.env`, код backend и SQL-файл не выдаются через HTTP; в архиве нет локального `.env` или рабочей БД.
- **9 passed** в существующих браузерных сценариях настоящего API, включая весь цикл анкеты, модерацию, альтернативы и мобильную доступность.
- Созданный тестовый профиль сохранился после остановки EXE и нового запуска с недоступным CSV.
- Изменяемые данные теста находились в отдельной временной папке; рабочие анкеты пользователя не использовались.

Размер ZIP: **19 067 954 байта**. SHA256:

```text
C58AEF088E43C24E39BF90C253B790B6FF55EDC0C8C21E1C7AD226108E06C815
```

Живые API-ключи не использовались в массовых тестах и не встроены в ZIP. Предыдущий живой OpenAI-вызов проверен на backend `c6e15d2`; алгоритм работы провайдера не изменён. Полная проверка на другом физическом компьютере, macOS/Linux и публичном интернет-хостинге в этот результат не входит.
