# Интеграция SQL-платформы

23.09.2026: по поручению капитана frontend `998ea25` объединён с backend `c6e15d2`. Все пять коммитов участника интерфейса включены; история авторства сохранена. Merge-коммит: `906df91`.

Первый агент отвечает за backend/SQL/API и слияние; участник 2 — frontend; участник 3 — независимая QA-приёмка. Актуальные правила API находятся в `contracts/platform-v2.md` и применяются в backend и обеих формах.

## Проверки объединённой версии

- Последний backend-прогон: **77 passed**. Включены исходные сценарии, удаление во время LLM-объяснения, правила длительности, повторная валидация при одобрении и запуск готовой SQL-базы без CSV.
- Последний полный HTTP-прогон: **73 passed** на отдельной временной БД. При интеграции исправлена одна устаревшая QA-фикстура: позитивный null-duration теперь проверяется на флористе. Запрет пустой длительности ведущего, фотографа и смешанных категорий проверяется backend-набором.
- Живой OpenAI на текущем backend с отдельной временной SQL-базой: **llm**, 3 карточки, **1226 мс** на вызов объяснений. Ключ и временная БД не входят в Git. Это замер одного вызова, не нагрузочный тест.
- Frontend unit/component: **59 passed**; TypeScript и production build прошли.
- Полный браузерный набор: **25 passed**, без пропусков; 16 сценариев с управляемыми ответами и 9 с настоящим API, включая цикл анкеты, доступность и администрирование. Использованы отдельные процессы, порты и временная БД. Платных запросов в этих тестах нет.

## Упрощённый запуск

- Реализован единый запуск `python run.py`: один процесс отдаёт собранный интерфейс и API. Код HTTP отделён от сервиса рекомендаций, конфигурация и изменяемая SQL-база отделены от поставляемых ресурсов.
- Собран Windows ZIP с Start.exe; девять браузерных сценариев настоящего API пройдены на распакованном EXE. Повторный запуск сохраняет данные. [Полный отчёт](distribution-verification.md), [инструкция](distribution.md), [архитектура](architecture.md).
- После архитектурных изменений backend/HTTP-набор: 156 passed. Установка зависимостей и сборка из новой отдельной копии также проверены.
- Дополнительная независимая приёмка участника QA продолжается; текущие результаты интеграции не заменяют его аудит.

Промпты в `docs/team-prompts/` отражают распределение исходных задач. Перед началом новой работы участникам следует получить актуальный main.
